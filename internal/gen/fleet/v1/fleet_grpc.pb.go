// Copyright The prometheus-mcp-fleet Authors.
// SPDX-License-Identifier: Apache-2.0

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.5.1
// - protoc             (unknown)
// source: fleet/v1/fleet.proto

// Package fleet.v1 defines the hub<->spoke control plane.
//
// Role reversal: the spoke dials the hub and, once the mTLS handshake
// completes, serves this gRPC service over the connection it opened. The hub
// is therefore the gRPC *client*. That inversion is what gives us native
// HTTP/2 stream multiplexing, per-request cancellation (RST_STREAM), deadline
// propagation (grpc-timeout) and flow control without writing any of it.

package fleetv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	SpokeService_Describe_FullMethodName = "/fleet.v1.SpokeService/Describe"
	SpokeService_Proxy_FullMethodName    = "/fleet.v1.SpokeService/Proxy"
)

// SpokeServiceClient is the client API for SpokeService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// SpokeService is served by the spoke over the tunnel it dialed to the hub.
type SpokeServiceClient interface {
	// Describe returns the spoke's cluster facts. The hub polls this on a timer
	// and passes the fingerprint it already holds; an unchanged spoke replies
	// with `unchanged: true` and no payload (~40 bytes on the wire).
	Describe(ctx context.Context, in *DescribeRequest, opts ...grpc.CallOption) (*DescribeResponse, error)
	// Proxy performs one allow-listed HTTP request against the spoke's local
	// Prometheus and streams the response body back in bounded chunks. The body
	// is opaque to the spoke: it never parses Prometheus JSON, which keeps the
	// spoke tiny and forward-compatible with Thanos/Mimir/Cortex query APIs.
	Proxy(ctx context.Context, in *ProxyRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[ProxyChunk], error)
}

type spokeServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewSpokeServiceClient(cc grpc.ClientConnInterface) SpokeServiceClient {
	return &spokeServiceClient{cc}
}

func (c *spokeServiceClient) Describe(ctx context.Context, in *DescribeRequest, opts ...grpc.CallOption) (*DescribeResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(DescribeResponse)
	err := c.cc.Invoke(ctx, SpokeService_Describe_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *spokeServiceClient) Proxy(ctx context.Context, in *ProxyRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[ProxyChunk], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &SpokeService_ServiceDesc.Streams[0], SpokeService_Proxy_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[ProxyRequest, ProxyChunk]{ClientStream: stream}
	if err := x.ClientStream.SendMsg(in); err != nil {
		return nil, err
	}
	if err := x.ClientStream.CloseSend(); err != nil {
		return nil, err
	}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type SpokeService_ProxyClient = grpc.ServerStreamingClient[ProxyChunk]

// SpokeServiceServer is the server API for SpokeService service.
// All implementations must embed UnimplementedSpokeServiceServer
// for forward compatibility.
//
// SpokeService is served by the spoke over the tunnel it dialed to the hub.
type SpokeServiceServer interface {
	// Describe returns the spoke's cluster facts. The hub polls this on a timer
	// and passes the fingerprint it already holds; an unchanged spoke replies
	// with `unchanged: true` and no payload (~40 bytes on the wire).
	Describe(context.Context, *DescribeRequest) (*DescribeResponse, error)
	// Proxy performs one allow-listed HTTP request against the spoke's local
	// Prometheus and streams the response body back in bounded chunks. The body
	// is opaque to the spoke: it never parses Prometheus JSON, which keeps the
	// spoke tiny and forward-compatible with Thanos/Mimir/Cortex query APIs.
	Proxy(*ProxyRequest, grpc.ServerStreamingServer[ProxyChunk]) error
	mustEmbedUnimplementedSpokeServiceServer()
}

// UnimplementedSpokeServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedSpokeServiceServer struct{}

func (UnimplementedSpokeServiceServer) Describe(context.Context, *DescribeRequest) (*DescribeResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method Describe not implemented")
}
func (UnimplementedSpokeServiceServer) Proxy(*ProxyRequest, grpc.ServerStreamingServer[ProxyChunk]) error {
	return status.Errorf(codes.Unimplemented, "method Proxy not implemented")
}
func (UnimplementedSpokeServiceServer) mustEmbedUnimplementedSpokeServiceServer() {}
func (UnimplementedSpokeServiceServer) testEmbeddedByValue()                      {}

// UnsafeSpokeServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to SpokeServiceServer will
// result in compilation errors.
type UnsafeSpokeServiceServer interface {
	mustEmbedUnimplementedSpokeServiceServer()
}

func RegisterSpokeServiceServer(s grpc.ServiceRegistrar, srv SpokeServiceServer) {
	// If the following call pancis, it indicates UnimplementedSpokeServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&SpokeService_ServiceDesc, srv)
}

func _SpokeService_Describe_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(DescribeRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(SpokeServiceServer).Describe(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: SpokeService_Describe_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(SpokeServiceServer).Describe(ctx, req.(*DescribeRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _SpokeService_Proxy_Handler(srv interface{}, stream grpc.ServerStream) error {
	m := new(ProxyRequest)
	if err := stream.RecvMsg(m); err != nil {
		return err
	}
	return srv.(SpokeServiceServer).Proxy(m, &grpc.GenericServerStream[ProxyRequest, ProxyChunk]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type SpokeService_ProxyServer = grpc.ServerStreamingServer[ProxyChunk]

// SpokeService_ServiceDesc is the grpc.ServiceDesc for SpokeService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var SpokeService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "fleet.v1.SpokeService",
	HandlerType: (*SpokeServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Describe",
			Handler:    _SpokeService_Describe_Handler,
		},
	},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "Proxy",
			Handler:       _SpokeService_Proxy_Handler,
			ServerStreams: true,
		},
	},
	Metadata: "fleet/v1/fleet.proto",
}
