# Copyright The prometheus-mcp-fleet Authors.
# SPDX-License-Identifier: Apache-2.0
#
# Minimum Gremlins test efficacy per package, in percent. Read by
# hack/mutation.sh, which fails a package scoring below its entry.
#
# Each entry is the measured score floored to a whole percent and reduced by 3.
# That margin is the tool's measured reproducibility, not padding: efficacy is
# killed/(killed+lived), and a mutant that merely runs slowly is recorded as
# TIMED OUT, leaving the numerator without leaving the denominator, so the score
# moves with machine load. internal/hub scored 92.93 / 90.54 / 89.71 across
# three runs of identical source. A baseline set to the best of those fails on
# any differently loaded machine, and a flaky gate teaches people to ignore it.
#
# Where a package sits below 100 the reason is recorded beside it. An
# equivalent mutant -- one that changes the source without changing anything a
# test could observe -- is a legitimate reason to stop. A test written to
# satisfy the tool rather than to describe the software is not.
#
# One entry does NOT meet that bar and says so: internal/mcptools carries 67
# survivors with no equivalence argument at all. They are accepted for now, not
# proven harmless. It is also the package where that matters most -- the
# agent-facing tool layer, where an unkilled mutant means a limit, a scope check
# or a truncation path does not do what its test claims. Treat that line as a
# debt marker, not as a finished result.
#
# The dominant equivalence class here: widening a boundary by one lands on a
# value where the guarded action is already idempotent. Clamping x to L when x
# already equals L, slicing a string to its own length, truncating a slice to
# its current length. No observable output differs, so no test can kill it.
#
# Raising an entry is a normal part of adding tests. Lowering one needs a
# reason in the commit message.
cmd/hub                     97   # measured 100.00
cmd/spoke                   97   # measured 100.00
internal/authn              93   # measured 96.92
internal/ca                 95   # measured 98.78
internal/certproof          74   # measured 77.78; 2 equivalent: both operators of the make([]byte,0,size) capacity hint in Transcript; append grows regardless
internal/clusterfacts       90   # measured 93.94; 10 equivalent: sentinels overwritten unconditionally later in the same function
internal/config             95   # measured 98.70
internal/fleet              97   # measured 100.00
internal/httpx              97   # measured 100.00
internal/hubcli             94   # measured 97.35; 3 equivalent: `if ttl > 0 { body.TTL = ttl }` at ttl 0 assigns the zero value the field already holds (omitempty either way)
internal/hub                96   # measured 97.20; 6 equivalent: two capacity hints, a threshold<=runway no-op at equality, and three wall-clock tie boundaries (cert expiry, revocation cache freshness x2) with no injectable clock
internal/hubapi             97   # measured 100.00
internal/kube               95   # measured 98.59; 1 unobservable: dialer KeepAlive inside a discarded closure, no exported hook
internal/mcpsurface         93   # measured 96.77; 2 equivalent: guarded nil-to-nil passthrough assignment
internal/mcptools           87   # measured 90.00; KNOWN GAP, not equivalence: 10 of 77 survivors are proven equivalent, the other 67 are simply unclassified
internal/obs                97   # measured 100.00
internal/promapi            94   # measured 97.62
internal/promclient         90   # measured 93.86; 5 equivalent (min-assign no-ops), 2 impractical: dialer timeouts in a discarded closure
internal/promproxy          94   # measured 97.62
internal/registry           90   # measured 93.42; 5 equivalent: s[:n] at len n is a no-op; levenshtein arg swap only picks the smaller row
internal/render             90   # measured 91.53; 20 equivalent, each documented in mutation_edges_test.go: guarded boundaries excluded by earlier invariants, and the roundSignificant group proven by exhaustive sweep or bit-identity (only lo bits are lost, unreachable within the promised exactness range)
internal/spoke              95   # measured 96.70; 7 equivalent, documented in util_test.go: if x<L {x=L} no-ops exactly at L, plus fullJitter bounds masked by invariants established earlier in the function
internal/store              96   # measured 99.07
internal/store/filestore    97   # measured 100.00
internal/store/secretstore  94   # measured 97.62; 1 equivalent: backoff cap assigns maxBackoff either way at the tie
internal/testutil           89   # measured 92.86
internal/token              97   # measured 100.00
internal/tunnel             95   # measured 98.35
internal/tunnel/grpctun     94   # measured 97.00
internal/tunnel/memtun      93   # measured 96.97
internal/tunnel/wstun       97   # measured 98.88; 1 equivalent: hello.Replicas 0 under omitempty is indistinguishable from unset. 23 timeouts make this score jittery; floor kept wide
internal/version            93   # measured 96.67
