# Copyright The prometheus-mcp-fleet Authors.
# SPDX-License-Identifier: Apache-2.0
#
# The shipped defaults, stated explicitly so a change to them shows up as a
# diff in this file's render rather than silently.
tunnel:
  serverNames:
    - hub.example.com
