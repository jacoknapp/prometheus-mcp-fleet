apiVersion: v2
name: prometheus-mcp-spoke
description: Tiny outbound-only agent that dials the prometheus-mcp-hub over an authenticated WebSocket and proxies this cluster's Prometheus HTTP API. One namespaced Role over one Secret, or no RBAC at all in memory identity mode.
type: application
version: 0.10.2
appVersion: "0.10.2"
kubeVersion: ">=1.28.0-0"
keywords:
  - mcp
  - model-context-protocol
  - prometheus
  - observability
  - ai
  - fleet
  - multi-cluster
home: https://github.com/jacoknapp/prometheus-mcp-fleet
sources:
  - https://github.com/jacoknapp/prometheus-mcp-fleet
maintainers:
  - name: jacoknapp
    email: jacoknapp@gmail.com
    url: https://github.com/jacoknapp
annotations:
  artifacthub.io/license: Apache-2.0
  artifacthub.io/operator: "false"
  artifacthub.io/prerelease: "true"
  artifacthub.io/signKey: |
    fingerprint: keyless (sigstore)
    url: https://github.com/jacoknapp/prometheus-mcp-fleet
