{{/*
Copyright The prometheus-mcp-fleet Authors.
SPDX-License-Identifier: Apache-2.0

Escape hatch for objects this chart does not model. Each entry is a complete
manifest and is passed through `tpl`, so it may reference .Values and .Release.
An Ingress here is rejected by `prometheus-mcp-spoke.validate`: the spoke's only
listener is the admin port.
*/}}
{{- range .Values.extraManifests }}
---
{{ tpl (toYaml .) $ }}
{{- end }}
